import React from "react";

export default class NavBar extends React.Component{
    constructor(props){
        super(props);
    }

    render(){
        const {t, isAuthorized, userInfo, loading, error} = this.props;
        return{
            
        }
    }
}